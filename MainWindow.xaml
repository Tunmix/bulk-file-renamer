<Window x:Class="BulkFileRenamer.MainWindow"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       Title="Bulk File Renamer" Height="500" Width="650"
       MinHeight="400" MinWidth="600"
       AllowDrop="True"
       Drop="Window_Drop"
       Background="{DynamicResource WindowBackground}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Menu -->
        <Menu Grid.Row="0"
              Background="{DynamicResource MenuBackground}"
              Foreground="{DynamicResource MenuForeground}">
            <MenuItem Header="Settings">
                <MenuItem Header="Ignore Rename Warning" IsCheckable="True" x:Name="IgnoreRenameWarningCheckBox"/>
                <MenuItem Header="Theme">
                    <MenuItem Header="System" IsCheckable="True" x:Name="ThemeIsSystemCheckBox"/>
                    <MenuItem Header="Dark Mode" IsCheckable="True" x:Name="ThemeIsDarkCheckBox"/>
                    <MenuItem Header="Light Mode" IsCheckable="True" x:Name="ThemeIsLightCheckBox"/>
                </MenuItem>
            </MenuItem>
        </Menu>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Browse Button + Folder Path -->
            <Button Name="BrowseButton" Grid.Row="0" Grid.Column="0" Content="" Width="120" Height="30" Margin="0,0,10,5" Click="BrowseButton_Click" Cursor="Hand" ToolTip="Select folder" Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource ButtonForeground}"></Button>
            <TextBox Name="FolderPathTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,5" VerticalContentAlignment="Center" Background="{DynamicResource TextBoxBackground}" Foreground="{DynamicResource TextBoxForeground}" BorderBrush="{DynamicResource TextBoxBorderBrush}" CaretBrush="{DynamicResource TextBoxCaretBrush}" FontSize="16"/>

            <!-- Starting Name -->
            <Label Grid.Row="1" Grid.Column="0" Content="Prefix:" VerticalAlignment="Center" Margin="0,0,10,5" Foreground="{DynamicResource LabelForeground}" Background="{DynamicResource LabelBackground}" FontSize="16"/>
            <TextBox Name="BaseNameTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,5" VerticalContentAlignment="Center" Text="renamed_" Background="{DynamicResource TextBoxBackground}" Foreground="{DynamicResource TextBoxForeground}" BorderBrush="{DynamicResource TextBoxBorderBrush}" CaretBrush="{DynamicResource TextBoxCaretBrush}" FontSize="16"/>

            <!-- Suffix -->
            <Label Grid.Row="2" Grid.Column="0" Content="Suffix:" VerticalAlignment="Center" Margin="0,0,10,5" Foreground="{DynamicResource LabelForeground}" Background="{DynamicResource LabelBackground}" FontSize="16"/>
            <TextBox Name="SuffixTextBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,5" VerticalContentAlignment="Center" Text="" Background="{DynamicResource TextBoxBackground}" Foreground="{DynamicResource TextBoxForeground}" BorderBrush="{DynamicResource TextBoxBorderBrush}" CaretBrush="{DynamicResource TextBoxCaretBrush}" FontSize="16"/>

            <!-- Checkboxes -->
            <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,5,0,5">
                <CheckBox Name="SkipHiddenCheckBox" Content="Skip Hidden/System Files" Margin="0,0,20,0" IsChecked="True" FontSize="16"/>
                <CheckBox Name="LeadingZerosCheckBox" Content="Use Leading Zeros" Margin="0,0,20,0" IsChecked="True" FontSize="16" />
                <CheckBox Name="KeepExtensions" Content="Keep file's extension" Checked="KeepExtensions_Checked" Unchecked="KeepExtensions_Unchecked" IsChecked="True"/>
            </StackPanel>

            <!-- Preview List -->
            <ListBox x:Name="PreviewListBox" Grid.Row="4" Grid.ColumnSpan="2" Margin="0,10,0,0" Visibility="Visible" Background="{DynamicResource ListBoxBackground}"
         Foreground="{DynamicResource ListBoxForeground}" BorderBrush="{DynamicResource ListBoxBorderBrush}"/>

            <!-- Start Button -->
            <Button Name="StartButton" Grid.Row="5" Grid.ColumnSpan="2" Content="RENAME" Height="60" Margin="0,10,0,0" Click="StartButton_Click" Cursor="Hand" FontSize="24" FontWeight="Bold" Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>

            <!-- Status Text -->
            <TextBlock Name="StatusTextBlock" Grid.Row="6" Grid.ColumnSpan="2" Text="" HorizontalAlignment="Center" Margin="0,10,0,0" FontWeight="Bold" Foreground="White"/>
        </Grid>
    </Grid>
</Window>
